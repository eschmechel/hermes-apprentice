global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: apprentice-proxy
    static_configs:
      - targets:
          - proxy:8083
    metrics_path: /metrics
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: proxy
